import SwiftUI

struct ModsContent: View {
    var body: some View {
        VStack {
            Text(NSLocalizedString("filter.category", comment: ""))
                .font(.headline)
            ScrollView {
                
            }
        }
    }
}

#Preview {
    ModsContent()
} 
