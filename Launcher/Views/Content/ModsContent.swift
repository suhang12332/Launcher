import SwiftUI

struct ModsContent: View {
    var body: some View {
        VStack {
            Text(NSLocalizedString("filter.category", comment: "分类筛选"))
                .font(.headline)
            ScrollView {

            }
        }
    }
}

#Preview {
    ModsContent()
}
